---
trigger: always_on
---

For styling, use DaisyUI and TailwindCSS.

You can use the files in 'example-code' as references.
- 'renderer.tsx' - an example of the root styling
- 'build-layout.tsx' - an example of a layout component
